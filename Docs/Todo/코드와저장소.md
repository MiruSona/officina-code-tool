# 할 일 : 코드 구조 · 저장소

> 담당 범위 : 대상 프로젝트에 넣어줄 코드 템플릿 · 컨벤션 · 저장소 MCP
> 전체 색인은 스튜디오 저장소(Officina)의 진행상황 문서에 있다.

## 1. 정해진 것

### 코드 구조

| 항목 | 결정 | 근거 |
| --- | --- | --- |
| 기본 구조 | **4층** — Master / Manager·Hub / Object / Agent·Util | `Todo/기본구조.md` |
| 앱 전체를 사는 층 | 위 4층 **위에 3층 추가** — `GameMaster`(static) / `GameRunner`(Mono) / `~Service`(순수 C#) | `Design/2026-08-20-기본구조설계.md` |
| Update 진입점 | **`GameRunner` 하나뿐.** `~Master` 는 Mono 지만 `Update` 를 안 쓴다 | 〃 |
| Mono 인 것 | `GameRunner` · `~Master` · `~Object` **셋뿐.** 나머지는 순수 C# | 〃 |
| 꺼내 쓰는 길 | 전역이든 씬이든 **`~Hub` 하나로 통일** | 〃 |
| 등록·해제 | 등록은 남이, **해제는 `OnDestroy` 에서 자기가** | 〃 |
| ECS/DOTS | **안 쓴다 (보류).** SpriteRenderer 가 컴패니언 컴포넌트라 2D 에서 이득이 적다 | `Research/2026-08-20-ECSDOTS구조조사.md` |
| 인스펙터 배선 | **흐름 배선은 금지, 모양은 프리팹 가능.** 까닭은 diff·검색·리뷰 (2026-09-21 전제 정정) | 스튜디오 저장소 공용 규칙 10번 |
| Unity | **Unity 6 LTS + 2D URP 확정** (게임마다 바꿀 수 있어야 함) | 스튜디오 저장소의 스튜디오키트 결정사항 (2026-08-20) |
| asmdef | 처음엔 **Runtime / Editor 2개만.** 기능별로 쪼개면 손해 | 〃 |
| 코드 폴더 | **기능별.** 그림·소리는 유형별 | 〃 |
| 네임스페이스 | 기본값 `Officina.*`, **설정으로 바꿀 수 있어야 한다** | 〃 |
| SOLID | **층을 나눠 본다.** 순수 C#(전투·인벤토리·상태·데이터) = 전부 적용 / MonoBehaviour = 컴포넌트 조합이 기본 | 〃 |
| Unity 조작 도구 | **지금은 안 깐다.** 배치모드 → 공식 CLI → uloop-cli → (마지막) MCP | 〃 |
| Init 순서 | **Master 의 등록 순서 그대로.** `Init`(자기 것만) / `PostInit`(Hub.Get 은 여기서만) 2단계 | `Design/2026-09-01-2차설계.md` |
| Agent vs Util | 상태 있고 owner 받으면 `~Agent`, 무상태면 `~Util`(**`static class`**). 옛 이름 Handler 를 Util 로 바꿈 | 〃 |
| `Hub.Get` 실패 | **예외를 던진다**(`HubNotFoundException`). 없을 수 있는 곳만 `TryGet`, 중복 등록도 예외 | 〃 |
| Process 중 목록 변경 | **버퍼 둘 + 즉시 dead 표시.** `ProcessList<T>` 하나만 쓴다 (추가는 다음 프레임, 제거는 순회 끝) | 〃 |
| 두 Hub 걸치기 | **금지.** 소유 Master 의 Hub 하나만 등록, 남의 것은 꺼내 쓴다. 양쪽에 늘 필요하면 Service | 〃 |
| Object 생성 | `Instantiate` 는 **ResourceService 한 곳뿐**, **전원 풀링**(`Rent`/`Return`), 키는 코드 상수 `PrefabKeys` | 〃 |
| Manager 끼리 알림 | **중앙 EventBus** — 타입 기반 · 동기 즉시 호출, 이벤트는 `readonly struct`. Hub 는 pull 전용 | 〃 |
| 끼우는 법 | **복사.** `Template/` 을 게임 `Assets/` 에 복사하고 `Officina.*` 만 치환. UPM 패키지 참조는 안 쓴다 | `Research/2026-09-18-복사방식끼우기조사.md` (사용자 확정 2026-09-18) |
| 복사 범위 | **`Assets/` 안까지** — 뼈대 `.cs` · asmdef 2 · `Default.globalconfig`. 폴더 트리·git 설정·서식 도구·애널라이저 DLL 은 뺀다 | `Design/2026-09-18-복사템플릿설계.md` |
| 덮어쓰기 | `.globalconfig` 만 덮는다(백업 먼저). `.cs`·asmdef 는 있으면 건너뜀. `.meta` 는 템플릿에 없다 | 〃 4-3 |

### 컨벤션

| 항목 | 결정 |
| --- | --- |
| 문서 개수 | **둘로 쪼갠다.** `코딩컨벤션.md`(사람·Claude용) + `코딩컨벤션-요약.md`(로컬 LLM 용, **100줄 이내**) |
| 핵심 경계 | **기계가 잡는 건 문서에 안 쓴다.** 규칙 개수만 늘어나 정작 중요한 게 묻힌다 |
| 자동 고침 | CSharpier (서식) |
| 설정 파일 | **`.globalconfig`** — Unity 가 `.editorconfig` 를 무시한다 (Won't Fix) |
| 애널라이저 | Microsoft.Unity.Analyzers (Unity 함정 43종), BannedApiAnalyzers (금지 API) |
| 문서에 쓸 것 | **이름을 무엇으로 지을지** — 아무도 못 잡는 것만 |
| 규칙 형태 | 규칙 한 줄 + 좋은 예 / 나쁜 예 한 쌍 |
| 기성 컨벤션 스킬·MCP | **안 쓴다.** 쓸 만한 게 없다 |
| 컨벤션 내용 | **1차 받음** — SOLID + 8가지. `Guide/코딩컨벤션.md` 에 정리됨. 나중에 더 준다 |

### 저장소 MCP

| 항목 | 결정 | 이유 |
| --- | --- | --- |
| AI 기억 저장소 | **아무것도 안 붙인다** | `Docs/` + git + `CLAUDE.md` + 오토 메모리로 충분 |
| 코드베이스 저장소 | **지금은 할 일 없음** | Unity 프로젝트가 없어 색인할 코드가 없다 |
| 둘을 합치기 | **안 된다** | 성격이 정반대. 코드 색인은 버려도 되지만 기억은 날리면 끝 |

## 2. 다음에 할 일

그림 툴 다음 순번이다. 지금 급한 게 아니다.

- [x] `기본구조.md` 의 애매한 곳 메꿔서 설계 문서로 승격 → `Design/2026-08-20-기본구조설계.md`
- [x] **2차 설계 — 1차에서 안 정한 것들** (근거 : 위 설계 문서 6장) → `Design/2026-09-01-2차설계.md`
- [x] `GameMaster` / `GameRunner` / `~Service` / `~Master` / `~Manager` / `~Hub` 의 C# 뼈대 코드 → `Template/_Project/Scripts/` 25장 (2026-09-18). **Unity 컴파일 확인은 사용자 몫 (U1)**
- [x] asmdef 2개 → `Template/` (2026-09-18). 폴더 트리·`.gitignore`·`.gitattributes` 는 범위에서 뺌 → `Guide/새저장소세우기.md` 로 남긴다 (아직 안 씀)
- [x] `Template/Default.globalconfig` (UNT 11종, 2026-09-18). 애널라이저 DLL 은 안 넣는다. **Unity 안에서 먹는지 미검증 — U2·U3 (`Design/2026-09-18-복사템플릿설계.md` 5-2)**
- [x] 컨벤션 문서 사람용 → `Guide/코딩컨벤션.md`
- [x] 컨벤션 요약본 (로컬 LLM 용, 100줄 이내) → `Guide/코딩컨벤션-요약.md`
- [ ] 코드베이스 저장소 — **Unity 프로젝트가 생긴 뒤에** 다시 본다

### 2026-09-21 사용 피드백 반영에서 남은 것

- [x] **템플릿 asmdef 의 TMP 참조** (2026-09-26). Runtime asmdef 가 `Unity.TextMeshPro` · `UnityEngine.UI` 를 이름으로 참조한다.
      Unity 6 에서도 이름이 같아 판을 가르지 않는다. Editor asmdef 는 TMP 를 직접 안 써서 그대로 둔다.
      **Unity 안 컴파일은 안 봤다. 이미 복사한 게임 저장소는 손으로 더해야 한다** (설치는 있는 asmdef 를 건너뛴다)
- [ ] **`Default.globalconfig` 진단이 IDE 에서 먹는지 확인.** 컴파일은 통과했지만 IDE 를 안 열어 봤다.
      `PrefabKeysValidator` 메뉴도 안 눌러 봤다. 근거 : `Todo/2026-09-18-ProjectMozzi설치피드백.md` 「안 한 것」

## 3. 아직 못 정한 것

- ~~키트 끼우는 법~~ → 복사로 확정 (2026-09-18). 남은 것 : `Guide/새저장소세우기.md` 쓰기 · Pester 시험(`Tests/Install.Tests.ps1`) · `ProcessList` 순수 C# 시험 · 서식 도구(CSharpier 대 `dotnet format`) · `*.png` LFS 여부
- 로컬 Qwen 접속 방식 (엔드포인트, MCP 연동)
- 이름 짓기 규칙 (컨벤션 2차)

## 2026-09-18 코드 리뷰 후속

전체 툴 코드 리뷰 판에서 이 툴은 발견 10건 중 8건을 고쳤다 (PS1 은 5경우 종료 코드 실측,
**C# 은 컴파일을 안 했다**). 아래가 남은 것이다.

### 먼저 할 것 — 사용자 Unity 실측에 다섯 파일 컴파일 확인을 더한다

이번에 고친 C# 다섯 파일이 **Unity 에서 컴파일되는지 아무도 안 봤다.**
`Docs/Todo/코드와저장소.md`·`기본구조.md` 의 사용자 실측 **U1~U5 에 아래를 더한다.**

| 파일 | 이번에 바뀐 것 |
| --- | --- |
| `Template/_Project/Scripts/Core/MasterBase.cs` | `OnDestroy` 예외 안전 · 매니저마다 `EventBus.UnsubscribeAllBy(manager)` |
| `Template/_Project/Scripts/Core/ProcessList.cs` | `Remove` 가 `_pendingAdd` 도 비움 · `Flush` 를 `finally` 로 |
| `Template/_Project/Scripts/Resource/ResourceService.cs` | Pop 뒤 파괴 검사 · 첫 대여와 풀 대여의 순서 맞춤 |
| `Template/_Project/Scripts/Core/GameRunner.cs` | `TimeScale` 을 `Process`·`LateProcess` 에만 적용 |
| `Template/_Project/Scripts/Core/GameMaster.cs` | 위 넷에 딸린 호출부 |

### 설계를 바꿔야 풀린다 — 별도 판 (사용자 결정 2026-09-18)

**C5 — 허브 등록의 소유권.** 지금은 `RegisterHubs`/`UnregisterHubs` 를 짝으로 부르는 것이
**관례일 뿐**이라 한쪽을 빠뜨려도 아무도 안 알려 준다. `MasterBase` 가 등록을 소유하게
바꿔야 한다(`SceneHub` 를 추상 속성으로 두는 식).

같이 볼 것 : `GlobalHub` 를 매니저가 **자기를 owner 로** 등록하면 C2 와 똑같은 구멍이 다시 난다.

### 보류 등급

- **C6** `Template/_Project/Scripts/Events/EventBus.cs:57` — `Publish` 가 매번 `ToArray` 라
  매 프레임 GC 가 돈다. `ProcessList` 처럼 인덱스로 도는 꼴로 바꾼다.

### 이번 판에서 새로 드러난 것

- ~~`install.ps1` 의 `ProjectVersion.txt` 파싱이 `m_EditorVersion:` 접두어째 찍힌다.~~ → 고침 (2026-09-21)
- **디렉터리 심링크 · UNC 경로 · 한글 경로를 안 재봤다.** C1·C7 은 정션까지만 실측했다.
  (실측으로 안 것 : PS 5.1 `Get-ChildItem -Recurse` 는 정션 안으로 **안 들어간다.**)
